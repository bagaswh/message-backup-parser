import { LocalesStore } from '../../../store/store-locales';

LocalesStore.defineLocale<LocaleLINE>('line', 'en', {
  firstLineSignature: 'Chat history with',
  beginningFile: 'Chat history with',
  dateSaved: 'Saved on'
});
